/* const os = require('os');

// userInfo
const user = os.userInfo();
console.log(user);

//system uptime seconds

const uptime = os.uptime;
console.log("th system uptime is " + uptime + ' seconds');

//currentOS
const currentOS={
    name:os.type(),
    release:os.release(),
    totalMem:os.totalmem(),
    freeMem:os.freemem(),
};
console.log(currentOS); */
